apiVersion: v1
kind: Service
metadata:
  name: {{ include "aiops-quality-project.fullname" . }}
  labels:
    {{- include "aiops-quality-project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aiops-quality-project.selectorLabels" . | nindent 4 }}